import 'package:flutter/material.dart';

class InboxNotification extends StatelessWidget {
  const InboxNotification({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
