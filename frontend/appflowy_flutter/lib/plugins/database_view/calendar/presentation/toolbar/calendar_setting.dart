import 'package:flutter/material.dart';

class CalendarSettingList extends StatelessWidget {
  const CalendarSettingList({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: 1000, height: 1000);
  }
}
