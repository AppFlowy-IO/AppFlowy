import 'package:flutter/material.dart';

class PageIconBottomSheet extends StatelessWidget {
  const PageIconBottomSheet({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
