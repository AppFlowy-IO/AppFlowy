import 'package:flutter/material.dart';

class MobileWorkspaceStartScreen extends StatelessWidget {
  const MobileWorkspaceStartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO(yijing): implement MobileWorkspaceStartScreen later
    return Scaffold(
      appBar: AppBar(
        title: const Text('MobileWorkspaceStartScreen'),
      ),
      body: const Placeholder(),
    );
  }
}
